import { ToggleIconDirective } from './toggle-icon.directive';

describe('ToggleIconDirective', () => {
  it('should create an instance', () => {
    const directive = new ToggleIconDirective();
    expect(directive).toBeTruthy();
  });
});
